# 产品需求文档（PRD）

**项目名称**：基于大语言模型与检索增强生成的智能菜谱推荐与规划系统
**文档版本**：V2.0
**编写日期**：2026-08-19
**适用阶段**：开发与验收（V2.0 依据当前实现补充营养/忌口/购物车/偏好/对话复用等功能）
**参考资料**：[开题.md](file:///f:/毕业设计/Project/开题.md)

---

## 目录

1. [项目概述](#1-项目概述)
2. [功能需求](#2-功能需求)
3. [非功能需求](#3-非功能需求)
4. [用户角色与用例](#4-用户角色与用例)
5. [系统架构](#5-系统架构)
6. [界面设计规范](#6-界面设计规范)
7. [数据模型](#7-数据模型)
8. [项目时间节点与里程碑](#8-项目时间节点与里程碑)
9. [风险评估及应对策略](#9-风险评估及应对策略)

---

## 1. 项目概述

### 1.1 项目背景

随着居民生活水平的提升与健康饮食观念的普及，家庭烹饪场景对菜谱的个性化、智能化推荐提出了更高要求。传统菜谱网站仅支持关键词检索与分类浏览，难以满足"减脂晚餐""20 元内三人餐"等自然语言需求；同时，烹饪爱好者缺少一个集菜谱创作、心得分享、智能问答于一体的协作平台。

本课题来源于企事业科研需求，旨在结合大语言模型（LLM）与检索增强生成（RAG）技术，构建一套面向家庭用户的智能菜谱推荐与菜单规划系统，为用户提供从"想吃什么"到"如何做出来"的完整闭环服务。

### 1.2 项目目标

| 维度 | 目标描述 |
|------|---------|
| **功能目标** | 实现菜谱服务、用户中心、互动分享、智能推荐、AI 烹饪助手、后台管理六大模块，覆盖开题报告全部研究内容 |
| **技术目标** | 基于 FastAPI + Vue3 + MySQL + Chroma + LangChain 的模块化架构，落地 RAG 语义检索与流式 AI 对话 |
| **用户体验目标** | 用户可通过自然语言一句话获取菜谱推荐或完整菜单搭配；AI 助手支持多轮对话与流式输出 |
| **质量目标** | 接口平均响应时间 < 500ms（非 AI 类），AI 流式首 token < 2s，系统可用性 ≥ 99% |

### 1.3 项目范围

**包含**：
- 用户端 Web 应用（含 PC 与移动端响应式适配）
- 管理端 Web 应用（与用户端共用同一前端，按角色路由切换）
- 后端 RESTful API 服务
- RAG 向量知识库构建与检索服务
- LLM 流式对话与推荐生成服务
- MySQL 关系型数据库与 Chroma 向量数据库

**不包含**：
- 原生移动 App（iOS/Android）
- 第三方支付、电商下单等商业化能力
- 视频菜谱上传与转码
- 多租户 SaaS 化运营

### 1.4 技术栈概览

| 层级 | 技术选型 |
|------|---------|
| 前端框架 | Vue 3.4 + Vue Router 4 + Pinia 2 |
| 前端 UI | Element Plus 2.7 + Element Plus Icons |
| 前端工具 | Vite 5、Axios、Marked（Markdown 渲染） |
| 后端框架 | Python FastAPI 0.136 + Starlette + Uvicorn |
| ORM 与数据库 | SQLAlchemy 2.0 + PyMySQL + MySQL 8（utf8mb4） |
| AI 框架 | LangChain 1.3 + ChromaDB 1.5 |
| LLM 服务 | 默认小米 Mimo mimo-v2.5（默认关闭深度思考，仅输出最终答案）；Embedding/Rerank 恒用 SiliconFlow |
| Embedding 服务 | SiliconFlow BAAI/bge-m3（1024 维，多语言） |
| Rerank 精排服务 | SiliconFlow BAAI/bge-reranker-v2-m3（交叉编码，两阶段检索第二级；复用 Embedding API Key） |
| 认证方案 | JWT（python-jose）+ bcrypt 密码哈希 |
| 流式协议 | SSE（Server-Sent Events，FastAPI `StreamingResponse` 手动实现；`data` 帧 + `[DONE]` 结束标记，会话 ID 走 `X-Conversation-Id` 响应头） |

### 1.5 名词术语

| 术语 | 含义 |
|------|------|
| RAG | Retrieval-Augmented Generation，检索增强生成；先从向量库检索相关文档，再交给 LLM 生成答案 |
| Embedding | 将文本转换为稠密向量，用于语义相似度计算 |
| Rerank（精排） | 交叉编码重排序模型；将查询与候选拼成文本对逐对细读打分，按相关度重排小候选集，弥补 Embedding 双塔结构交互浅的精度上限 |
| Top-K | 向量检索返回的最相似文档数量；对话场景先召回 K≈40 再按菜谱去重，最终候选池上限 12 道 |
| 套餐（Meal Plan） | 多道菜组合而成的完整菜单，如"工作日三人晚餐" |
| SSE | Server-Sent Events，服务端推送技术，用于 AI 流式输出 |
| 软删除 | 通过 `is_deleted=1` 标记而非物理删除，保护数据完整性 |
| 营养估算 | 依据食材每 100g 营养值与用量解析结果，估算菜谱每份的 kcal/蛋白质/脂肪/碳水 |
| 忌口过滤 | 依据用户偏好的忌口/过敏原标签（海鲜/坚果/奶制品/蛋/麸质等）过滤或提示菜谱 |
| 菜谱购物车 | 浏览时"加购"菜谱，持久化到 localStorage，一键生成套餐 |

---

## 2. 功能需求

### 2.1 功能模块全景

系统按开题报告划分为六大模块，共计 44 项功能点：

```
┌──────────────────────────────────────────────────────────────┐
│                       智能菜谱推荐系统                          │
├──────────┬──────────┬──────────┬──────────┬──────────┬───────┤
│ 菜谱服务  │ 用户中心  │ 互动模块  │ 智能推荐  │ AI助手   │ 后台  │
│  模块    │  模块    │  模块    │  模块    │  模块    │ 管理  │
└──────────┴──────────┴──────────┴──────────┴──────────┴───────┘
```

### 2.2 核心功能（P0）

#### 2.2.1 菜谱服务模块（FR-RECIPE）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-R01 | 菜谱列表 | 分页展示已审核菜谱，支持关键词搜索、难度多选、标签筛选、预算区间筛选；排序支持「字段（时间/价格/难度）× 方向（升/降）」自由组合 | P0 |
| FR-R02 | 菜谱详情 | 展示菜谱标题、封面、描述、标签、食材列表、烹饪步骤、浏览/收藏数、作者信息；自动记录浏览历史 | P0 |
| FR-R03 | 菜谱创建 | 登录用户可创建菜谱（基础信息+食材+步骤+标签），普通用户提交后进入 `pending` 审核状态，管理员直接 `approved` | P0 |
| FR-R04 | 菜谱编辑 | 作者可编辑自己的菜谱，采用"先删后插"策略更新关联数据 | P0 |
| FR-R05 | 菜谱删除 | 作者可软删除自己的菜谱（`is_deleted=1`） | P0 |
| FR-R06 | 本周热门 | 基于近 7 天浏览次数统计返回热门菜谱；本周无数据时回退到总浏览数排序 | P0 |
| FR-R07 | 向量同步 | 菜谱创建/更新时自动同步到 Chroma 向量库，支持全量重建与断点续传 | P0 |
| FR-R08 | 营养信息与份量估算 | 菜谱详情展示按份估算的热量/蛋白质/脂肪/碳水；用量自由文本自动解析为克数 | P1 |
| FR-R09 | 忌口/过敏提示 | 依据食材 `diet_tags` 与用户偏好，在菜谱详情与推荐中提示或过滤触碰忌口的菜谱 | P1 |

**验收标准**：
- 列表接口支持 ≥4 维度联合筛选，响应时间 < 300ms
- 详情接口使用 `joinedload` 避免N+1查询
- 封面图缺失时回退到 `/static/recipe_covers/default.jpg`
- 营养估算：食材均无营养数据时应返回空并隐藏该模块，不报错

#### 2.2.2 AI 烹饪助手模块（FR-AI）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-A01 | 流式多轮对话 | 用户发送自然语言问题，系统经两阶段检索（Embedding 粗排召回 → Rerank 交叉编码精排）构建菜谱候选池后交由 LLM 生成回答，使用 SSE 实时流式输出 | P0 |
| FR-A02 | 会话管理 | 每次对话自动创建/续接会话（`ai_conversations`），消息持久化到 `ai_messages` 表 | P0 |
| FR-A03 | 历史对话查询 | 用户可查看历史会话列表与消息内容 | P0 |
| FR-A04 | 会话删除 | 用户可删除自己的会话，同时清理内存中的对话上下文 | P0 |
| FR-A05 | 食材替代问答 | 基于菜谱知识库回答食材替代、烹饪技巧等问题 | P0 |
| FR-A06 | 单菜推荐 | 用户描述需求（如"清淡的素菜"），AI 返回单菜推荐 | P0 |
| FR-A07 | 多菜搭配建议 | 用户描述场景（如"减脂晚餐"），AI 推荐多菜组合套餐 | P0 |
| FR-A08 | 会话历史复用 | 在"AI 对话历史"页对某会话点"继续对话"可载入历史续聊；抽屉内可"开始新对话"；支持跨会话上下文续接 | P1 |

**验收标准**：
- 流式首 token 响应 < 2s
- 支持 SSE 协议，前端可逐字渲染
- AI 回复失败时返回降级提示，不阻断候选结果展示
- 对话历史支持跨会话续接，上下文窗口按 token 数自动截断
- 默认多轮对话；可从"对话历史"继续指定会话，也可一键开启新会话（不自动续接）

#### 2.2.3 智能推荐模块（FR-REC）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-REC01 | 个性化推荐 | 已登录用户基于收藏与浏览历史做 RAG 语义推荐；无行为数据的新号按标签轮转走冷启动多样推荐（非空、覆盖多种菜系）；忌口过滤前置到各候选阶段，未登录返回空由前端热门替代 | P0 |
| FR-REC02 | 预算推荐 | 按用户预算金额筛选菜谱，可选餐次类型过滤；在预算内自动生成 2 菜/3 菜组合（贪心 + 成本剪枝） | P0 |
| FR-REC03 | 自然语言推荐 | 用户输入自然语言描述，系统通过 RAG 检索菜谱并返回 top-K 候选 | P0 |
| FR-REC04 | 流式智能推荐 | 推荐能力统一收敛到右下角「AI 助手」多轮对话（`/api/ai/chat`，对话中借 RAG 检索相关菜谱后由 LLM 流式生成推荐语）与菜谱列表「智能搜索」（`/api/recommendations/query`）两级入口；会话内仅保存用户的真实输入，不落盘内部系统提示词 | P0 |

**验收标准**：
- RAG 检索失败时降级到关键词 n-gram 评分匹配，保证接口不报错
- 推荐结果支持预算约束合并去重
- 菜谱列表「智能搜索」（`/query`）以 JSON 返回候选列表；AI 助手对话以 SSE 流式返回推荐内容
- 对话记录中的用户消息仅含用户输入（不含"请根据候选列表..."等系统提示词）；首页不再展示「AI 智能推荐」入口，AI 对话统一经右下角悬浮 AI 助手入口

### 2.3 次要功能（P1/P2）

#### 2.3.1 用户中心模块（FR-USER）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-U01 | 用户注册 | 用户名+邮箱+密码注册，密码 bcrypt 哈希存储 | P0 |
| FR-U02 | 用户登录 | 校验成功后签发 JWT（24h 有效期） | P0 |
| FR-U03 | 个人信息 | 查看/修改昵称、头像、邮箱 | P1 |
| FR-U04 | 收藏管理 | 收藏/取消收藏菜谱或套餐，支持列表查看 | P1 |
| FR-U05 | 浏览历史 | 自动记录菜谱/套餐浏览历史，支持列表查看 | P1 |
| FR-U06 | AI 对话历史 | 查看历史会话与消息内容，支持删除与"继续对话" | P1 |
| FR-U07 | 我的菜谱 | 展示我创建的菜谱（含 pending/rejected 状态与驳回意见），支持编辑/删除/重新提交 | P1 |
| FR-U08 | 我的套餐 | 展示我创建的套餐（含审核状态），支持编辑/删除 | P1 |
| FR-U09 | 饮食偏好设置 | 设置偏好菜系（cuisines）与忌口/过敏标签（diet_tags，JSON 持久化），用于过滤与推荐 | P1 |
| FR-U10 | 菜谱合集（购物车） | 浏览菜谱时可收集进"菜谱合集"，持久化到 localStorage，一键基于合集生成套餐 | P2 |
| FR-U11 | 待做清单 | 与收藏、菜谱合集语义独立的"近期要做"清单；菜谱卡片可一键加入，个人中心页集中查看/移除（localStorage 持久化） | P2 |

#### 2.3.2 互动模块（FR-INTERACT）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-I01 | 菜谱点评 | 用户对菜谱发表评分（1-5）与文字评论，支持软删除 | P1 |
| FR-I02 | 烹饪心得发布 | 用户发布烹饪心得（标题+正文+关联菜谱+图片），可设公开/私有 | P1 |
| FR-I03 | 烹饪心得浏览 | 心得列表与详情页，公开心得自动同步到 RAG 向量库 | P1 |
| FR-I04 | 套餐创建 | 用户从菜谱库选择多道菜组合成套餐，可设公开/私有 | P1 |
| FR-I05 | 套餐浏览 | 套餐列表与详情，普通用户提交的套餐需审核后公开 | P1 |
| FR-I06 | 套餐收藏 | 收藏他人公开套餐 | P2 |

#### 2.3.3 后台管理模块（FR-ADMIN）

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|---------|---------|--------|
| FR-ADM01 | 管理后台首页 | 展示待审核数量、用户数、菜谱数等统计指标 | P1 |
| FR-ADM02 | 菜谱审核 | 审核用户提交的菜谱，通过/驳回（含驳回意见） | P0 |
| FR-ADM03 | 套餐审核 | 审核用户提交的套餐 | P0 |
| FR-ADM04 | 菜谱管理 | 管理员可对菜谱进行增、删、改、查 | P0 |
| FR-ADM05 | 用户管理 | 查看/禁用用户、调整角色 | P1 |
| FR-ADM06 | 标签管理 | 标签的增删改查 | P1 |
| FR-ADM07 | 食材管理 | 食材基础库的增删改查 | P1 |

### 2.4 功能需求清单（汇总）

| 模块 | P0 数量 | P1 数量 | P2 数量 | 小计 |
|------|--------|--------|--------|------|
| 菜谱服务 | 7 | 2 | 0 | 9 |
| 用户中心 | 2 | 7 | 1 | 10 |
| 互动模块 | 0 | 5 | 1 | 6 |
| 智能推荐 | 4 | 0 | 0 | 4 |
| AI 烹饪助手 | 7 | 1 | 0 | 8 |
| 后台管理 | 3 | 4 | 0 | 7 |
| **合计** | **23** | **19** | **2** | **44** |

---

## 3. 非功能需求

### 3.1 性能需求

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| 普通接口平均响应时间 | ≤ 300ms | 接口压测（wrk/ab） |
| 菜谱列表查询（10万级数据） | ≤ 500ms | 已建立 `idx_status`、`idx_estimated_cost`、`idx_created_at` 索引 |
| 菜谱详情接口（含联表加载） | ≤ 300ms | 使用 `joinedload` 预加载避免 N+1 |
| AI 流式首 token 响应 | ≤ 2s | SSE 连接建立到首个 chunk 到达 |
| Embedding 单批次编码（64 条） | ≤ 5s | SiliconFlow API 实测 |
| 向量检索 Top-5 | ≤ 200ms | Chroma 本地查询 |
| Rerank 精排（单轮对话，约 20~30 候选对） | ≤ 1s | SiliconFlow API 实测；超时（默认 10s）自动降级为召回排序 |
| 并发支持 | ≥ 100 QPS | Uvicorn 多 worker 部署 |
| 前端首屏加载 | ≤ 2s | Vite 构建 + 路由懒加载 |

### 3.2 安全需求

| 类别 | 要求 |
|------|------|
| **密码安全** | 使用 bcrypt 哈希存储，禁止明文存储；注册接口对密码强度做基本校验 |
| **身份认证** | JWT 令牌认证，HS256 签名；令牌有效期 24 小时；密钥通过环境变量注入 |
| **权限控制** | 基于 `role` 字段（user/admin）实现 RBAC；作者权限通过 `author_id` 校验 |
| **接口防护** | 所有写操作要求登录；管理接口要求 admin 角色；用户中心接口校验资源所有权 |
| **CORS 策略** | 开发期允许所有来源，**生产环境必须配置白名单域名** |
| **SQL 注入防护** | 全部使用 SQLAlchemy ORM，禁止字符串拼接 SQL |
| **XSS 防护** | 用户输入的富文本（心得、评论）使用前端 `marked` 渲染时启用 sanitize |
| **密钥管理** | LLM/Embedding API Key、JWT Secret 必须存放在 `.env` 文件，禁止入库 git |
| **图片安全** | 菜谱封面图仅允许来自专业美食网站白名单（meishichina.com、xiachufang.com、douguo.com、xiangha.com），且必须使用 HTTPS |
| **审核机制** | 用户提交的菜谱与套餐必须经管理员审核才能公开 |

### 3.3 可靠性需求

- **RAG 容错降级**：Embedding API 失败时返回零向量兜底；Chroma 检索失败时降级到 metadata 关键词评分排序
- **Rerank 精排降级**：精排 API 失败/超时/未配置 Key 时返回空，候选池自动退回召回原序，主链路不受影响；可通过 `RERANK_ENABLED=false` 一键关闭
- **AI 服务降级**：LLM 流式对话失败时不阻断候选菜谱展示，返回降级提示文案
- **向量同步容错**：菜谱创建/更新时向量同步失败不影响主流程（try/except 兜底）
- **断点续传**：全量向量库重建支持 checkpoint，中断后可从断点继续，避免重复消耗 API 额度
- **软删除策略**：菜谱、心得、评论均使用 `is_deleted` 软删除，保护数据完整性
- **数据库事务**：多表写入使用 SQLAlchemy session 事务，失败自动回滚

### 3.4 可用性需求

- 系统可用性目标 ≥ 99%（单机部署）
- 提供健康检查端点 `/api/health` 供监控探测
- 静态资源与 API 服务可独立部署/扩容
- 错误响应统一格式：`{"code": int, "message": str, "detail": any}`

### 3.5 可维护性需求

- 后端按 `api / services / models / schemas / core / utils` 分层，模块间低耦合
- API 路由按业务模块拆分，统一以 `/api/` 为前缀
- 配置项集中管理于 `app/config.py`，通过 Pydantic Settings 从 `.env` 读取
- 关键代码（如 RAG、AI 服务）配套"面向答辩"的设计说明注释
- FastAPI 自动生成 OpenAPI 文档（`/docs`），便于前后端协作

### 3.6 兼容性需求

| 维度 | 要求 |
|------|------|
| 浏览器 | Chrome 100+、Edge 100+、Firefox 100+、Safari 15+ |
| 分辨率 | 1280×800 及以上桌面端优先；移动端响应式适配（≥375px） |
| 操作系统 | 服务端支持 Linux/Windows；客户端跨平台 |
| Python 版本 | 3.10+ |
| Node 版本 | 18+ |
| MySQL 版本 | 8.0+（需支持 utf8mb4） |
| 字符编码 | 全链路 utf8mb4，支持 emoji 等 4 字节 UTF-8 字符 |

### 3.7 可扩展性需求

- Embedding 模型与 LLM 通过工厂函数注入，便于切换供应商
- Chroma 向量库采用单例模式，支持后续替换为 Milvus/Qdrant
- RAG 文档来源（菜谱/心得）通过 `source_type` metadata 区分，可扩展更多类型
- 推荐策略封装在 `recommendation_service`，便于新增算法

---

## 4. 用户角色与用例

### 4.1 用户角色定义

| 角色 | 标识 | 权限范围 |
|------|------|---------|
| **游客** | 未登录 | 浏览公开菜谱、套餐、心得；使用自然语言推荐；查看 AI 推荐候选（不可发起 AI 对话） |
| **普通用户** | `role=user` | 游客权限 + 收藏/浏览历史管理 + 创建菜谱/心得/套餐 + AI 多轮对话 + 评论评分 + 我的菜谱/我的套餐/饮食偏好设置 + 菜谱购物车 |
| **管理员** | `role=admin` | 普通用户权限 + 菜谱/套餐审核 + 用户管理 + 标签/食材管理 + 后台统计 |

### 4.2 核心用例图（文字描述）

```
                        ┌─────────────┐
                        │   游客      │
                        └──────┬──────┘
                               │
            ┌──────────────────┼──────────────────┐
            ▼                  ▼                  ▼
      浏览菜谱列表        查看菜谱详情        自然语言推荐
      浏览套餐列表        查看心得详情        （仅候选，无AI对话）

                        ┌─────────────┐
                        │  普通用户    │
                        └──────┬──────┘
                               │
   ┌───────────┬───────────────┼───────────────┬───────────┐
   ▼           ▼               ▼               ▼           ▼
 注册/登录   收藏菜谱        创建菜谱        AI多轮对话   发布心得
 修改资料   查看历史        创建套餐        食材替代问答  发表点评

                        ┌─────────────┐
                        │   管理员    │
                        └──────┬──────┘
                               │
                ┌──────────────┼──────────────┐
                ▼              ▼              ▼
            菜谱审核       套餐审核        用户管理
            标签管理       食材管理        后台统计
```

### 4.3 关键用例描述

#### UC-01：自然语言智能推荐（流式）

| 字段 | 内容 |
|------|------|
| **用例 ID** | UC-01 |
| **参与者** | 普通用户 |
| **前置条件** | 用户已登录 |
| **主流程** | 1. 用户在右下角「AI 助手」对话中输入自然语言需求（如"设计一顿减脂晚餐，预算 30 元"）<br>2. 前端调用 `POST /api/ai/chat`（多轮对话），AI 结合 RAG 检索结果流式生成推荐<br>3. 后端通过 RAG 检索菜谱，合并预算约束等上下文<br>4. LLM 流式生成推荐语，逐 token 推送，结束后保存 AI 回复<br>5. 流结束收到 `[DONE]` 标记<br>6. 对话保存在「个人中心 → AI 对话记录」，用户消息仅含真实输入，不含内部系统提示词 |
| **备选流程** | - RAG 检索失败 → 降级关键词匹配<br>- LLM 调用失败 → 返回友好降级提示，不崩溃<br>- 预算为空 → 跳过预算合并步骤<br>- 游客（未登录）→ 可通过菜谱列表「智能搜索」获取候选，但不发起 AI 对话 |
| **后置条件** | 会话与完整消息持久化；用户可在 AI 对话记录中随时回看并继续多轮追问 |

#### UC-02：AI 烹饪助手多轮对话

| 字段 | 内容 |
|------|------|
| **用例 ID** | UC-02 |
| **参与者** | 普通用户 |
| **前置条件** | 用户已登录 |
| **主流程** | 1. 用户打开 AI 助手对话框<br>2. 输入问题（如"鸡蛋怎么打发更稳定？"）<br>3. 前端调用 `POST /api/ai/chat`，SSE 接收流式回复<br>4. 后端从 DB 加载会话历史到内存，保存用户消息<br>5. 通过 RAG 检索相关菜谱/心得作为上下文<br>6. LLM 流式生成回答，逐 chunk 推送<br>7. 流结束后保存 AI 回复到 `ai_messages` 表 |
| **后置条件** | 对话完整显示；可在"AI 对话历史"页面查看 |

#### UC-03：用户创建并提交菜谱

| 字段 | 内容 |
|------|------|
| **用例 ID** | UC-03 |
| **参与者** | 普通用户 |
| **前置条件** | 用户已登录 |
| **主流程** | 1. 用户点击"创建菜谱"<br>2. 填写标题、描述、封面、难度、烹饪时间、份量、预算<br>3. 选择/新建标签，添加食材与用量，添加烹饪步骤<br>4. 提交后调用 `POST /api/recipes`<br>5. 后端设置 `status=pending`，写入主表与关联表<br>6. 同步到 Chroma 向量库（失败不阻断）<br>7. 等待管理员审核通过后公开可见 |
| **后置条件** | 菜谱在"我的菜谱"中可见（pending 状态）；管理员审核通过后公众可见 |

#### UC-04：管理员审核菜谱

| 字段 | 内容 |
|------|------|
| **用例 ID** | UC-04 |
| **参与者** | 管理员 |
| **前置条件** | 管理员已登录后台 |
| **主流程** | 1. 管理员进入"菜谱审核"页面<br>2. 查看待审核菜谱列表（`status=pending`）<br>3. 点开详情查看内容<br>4. 点击"通过"或"驳回"（填写驳回意见）<br>5. 后端更新 `status`、`reviewer_id`、`review_comment`、`reviewed_at` |
| **后置条件** | 通过的菜谱对公众可见；驳回的菜谱作者可在"我的菜谱"看到驳回意见并修改重新提交 |

---

## 5. 系统架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                         前端层（Vue3）                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  路由层   │  │  状态层   │  │  视图层   │  │  API层   │     │
│  │ (Router) │  │ (Pinia)  │  │ (Views)  │  │ (Axios)  │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTP / SSE
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端层（FastAPI）                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  中间件层：CORS / 异常处理 / 日志                       │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  API 路由层（按业务模块拆分）                          │   │
│  │  auth│recipes│users│reviews│cooking-notes│meal-plans│   │
│  │  ai│recommendations│admin                              │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  依赖注入层：get_current_user / get_optional_user     │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  服务层：ai_service / rag_service / recommendation    │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  数据访问层：SQLAlchemy ORM / Chroma Client           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌────────────────┐
│  MySQL 8.0   │  │  Chroma DB   │  │  外部 AI 服务    │
│ (16张关系表)  │  │ (本地持久化)  │  │ Mimo + 硅基流动 │
└──────────────┘  └──────────────┘  └────────────────┘
                          │
                          ▼
                  ┌──────────────┐
                  │ Embedding API│
                  │  BGE-M3 1024d│
                  └──────────────┘
```

### 5.2 后端分层架构

| 层级 | 目录 | 职责 |
|------|------|------|
| 入口层 | `app/main.py` | FastAPI 应用工厂、中间件注册、路由挂载、静态文件服务 |
| 配置层 | `app/config.py` | Pydantic Settings 集中管理所有配置（DB/JWT/LLM/RAG） |
| API 路由层 | `app/api/*.py` | RESTful 接口定义、参数校验、依赖注入 |
| Schema 层 | `app/schemas/*.py` | Pydantic 数据验证模型（请求/响应体） |
| 服务层 | `app/services/*.py` | 核心业务逻辑（AI 对话、RAG 检索、推荐引擎） |
| 工具层 | `app/utils/*.py` | 营养估算（nutrition）、用量解析（portions）、忌口/过敏过滤（recipe_diet）等通用工具 |
| 模型层 | `app/models/` | SQLAlchemy ORM 模型（16 张表） |
| 基础设施层 | `app/core/` | JWT 处理、密码哈希、依赖注入工具 |
| 数据库层 | `app/database.py` | 引擎与 Session 管理 |

### 5.3 中间件管道

```
请求 → CORS 中间件 → 路由匹配 → 依赖注入（鉴权） → 路由处理函数 → 响应
```

### 5.4 RAG 两阶段检索流程（对话场景）

```
用户查询（AI 对话入口）
   │
   ▼
┌─────────────────────┐
│ 第一级：Embedding 粗排召回 │ SiliconFlow BGE-M3 (1024维，双塔 bi-encoder)
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ Chroma 余弦相似度检索 │ 召回较多候选分块（默认40），仅菜谱 source_type=recipe
└─────────┬───────────┘
          │
          ├─失败/无结果 ─→ 降级：拉取元数据+原文，关键词 n-gram 评分排序
          │              （整词：标题+3.0/标签+1.5/正文+0.8；2-3字gram：标题0.6/标签0.3/正文0.15）
          │
          ▼
┌─────────────────────┐
│ 按 source_id 去重     │
└─────────┬───────────┘
          │ 候选过少 → 热门菜谱补齐（按收藏/浏览降序，保证非空）
          ▼
┌─────────────────────┐
│ 第二级：Rerank 精排    │ bge-reranker-v2-m3 交叉编码，「查询×每道候选」拼对逐对打相关分
└─────────┬───────────┘
          │ 分数融合重排：α×精排分+(1-α)×召回位置分（RERANK_ALPHA=0.5 微调）
          │ 高相关菜上浮、兜底/沾边菜沉底；失败/超时/未启用→降级为召回原序
          ▼
┌─────────────────────┐
│ 忌口过滤前置          │ 硬性剔除触忌口菜谱；全部触忌口→放宽并备注（为他人做菜时不硬拦）
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 预算感知排序          │ 识别预算→预算内菜品靠前（精排激活时组内保持相关度序），注入"尽量用足预算(90%~100%)"提示
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│ 紧凑候选池            │ 菜名/预估成本/食材/标签/短简介，去重后上限12道
└─────────┬───────────┘
          │ 动态内容置于请求尾部；静态系统提示词作稳定前缀（利于缓存）
          ▼
┌─────────────────────┐
│ LLM 流式生成         │ 小米 Mimo mimo-v2.5（关闭深度思考）
└─────────┬───────────┘
          │
          ▼
   SSE 推送前端（data 帧 + [DONE]，会话ID走 X-Conversation-Id 头）
```

### 5.5 文档分块与同步策略

| 项目 | 参数/策略 |
|------|----------|
| 分块器 | `RecursiveCharacterTextSplitter` |
| chunk_size | 500 字符 |
| chunk_overlap | 50 字符 |
| 分隔符优先级 | `\n\n` > `\n` > `。` > `，` > `；` > 空格 |
| Embedding 批次 | 64 条/批 |
| 增量同步 | 菜谱/心得创建或更新时实时同步，"先删后插"保证幂等 |
| 全量重建 | 支持 checkpoint 断点续传，每批次成功后立即写入 checkpoint |
| 文档结构 | content=结构化文本，metadata={source_type, source_id, title, tags} |

### 5.6 部署架构

```
┌─────────────────────────────────────────┐
│             开发/单机部署                  │
│  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │  Vite Dev │  │ Uvicorn  │  │ MySQL  │ │
│  :3000   │  │  :8000   │  │ :3308  │ │
│  └──────────┘  └──────────┘  └────────┘ │
│         │           │                    │
│         └─ Chroma 本地持久化目录 ──────── │
└─────────────────────────────────────────┘
                  │
                  ▼
        ┌──────────────────┐
        │ Mimo API (LLM)   │
        │ SiliconFlow API  │
        │ (Embedding+精排) │
        └──────────────────┘
```

---

## 6. 界面设计规范

### 6.1 整体风格

- **设计风格**：扁平化 + 卡片式布局，强调内容可读性
- **主色调**：温暖橙色系（#FF6B35 系列）作为品牌色，呼应"美食"主题
- **辅助色**：中性灰 (#606266)、成功绿 (#67C23A)、警告橙 (#E6A23C)、危险红 (#F56C6C)
- **字体**：默认系统字体栈；正文 14px，标题 18-24px
- **圆角**：卡片 8px，按钮 4px

### 6.2 页面结构

#### 6.2.1 全局布局（MainLayout）

```
┌─────────────────────────────────────────────────┐
│  顶部导航栏（Logo + 菜单 + 用户头像/登录按钮）      │
├─────────────────────────────────────────────────┤
│                                                 │
│                 <router-view />                 │
│                                                 │
├─────────────────────────────────────────────────┤
│                  底部版权信息                      │
└─────────────────────────────────────────────────┘
```

#### 6.2.2 页面清单

| 页面 | 路径 | 角色权限 | 说明 |
|------|------|---------|------|
| 首页 | `/` | 公开 | 热门菜谱 + 个性化推荐 + 自然语言推荐入口 + AI 助手入口 |
| 菜谱列表 | `/recipes` | 公开 | 多维度筛选 + 卡片列表 + 分页 |
| 菜谱详情 | `/recipes/:id` | 公开 | 封面 + 信息 + 食材 + 步骤 + 评论 + 相关推荐 |
| 菜谱创建 | `/recipes/create` | 登录用户 | 表单 + 食材/步骤动态添加 |
| 菜谱编辑 | `/recipes/:id/edit` | 作者 | 复用创建表单 |
| 心得列表 | `/cooking-notes` | 公开 | 卡片列表 |
| 心得详情 | `/cooking-notes/:id` | 公开 | Markdown 渲染 + 关联菜谱 |
| 套餐列表 | `/meal-plans` | 公开 | 卡片列表 |
| 套餐详情 | `/meal-plans/:id` | 公开 | 套餐信息 + 包含菜品列表 |
| 套餐创建 | `/meal-plans/create` | 登录用户 | 选择菜品 + 套餐信息 |
| 登录 | `/login` | 公开 | 用户名/邮箱 + 密码 |
| 注册 | `/register` | 公开 | 用户名 + 邮箱 + 密码 |
| 个人资料 | `/user/profile` | 登录用户 | 修改昵称/头像/邮箱 |
| 我的收藏 | `/user/favorites` | 登录用户 | 菜谱/套餐收藏分类展示 |
| 浏览历史 | `/user/history` | 登录用户 | 时间倒序浏览记录 |
| 我的菜谱 | `/user/my-recipes` | 登录用户 | 我创建的菜谱（含审核状态、驳回意见） |
| 我的套餐 | `/user/my-meal-plans` | 登录用户 | 我创建的套餐（含审核状态） |
| 饮食偏好 | `/user/preferences` | 登录用户 | 偏好菜系与忌口/过敏标签设置 |
| AI 对话历史 | `/user/conversations` | 登录用户 | 会话列表 + 消息查看 + 继续对话 |
| 后台首页 | `/admin/dashboard` | 管理员 | 统计卡片 + 待审核提醒 |
| 菜谱审核 | `/admin/recipe-audit` | 管理员 | 待审核列表 + 审核操作 |
| 套餐审核 | `/admin/meal-plan-audit` | 管理员 | 待审核列表 + 审核操作 |
| 用户管理 | `/admin/users` | 管理员 | 用户列表 + 状态/角色操作 |
| 标签管理 | `/admin/tags` | 管理员 | 标签 CRUD |
| 食材管理 | `/admin/ingredients` | 管理员 | 食材 CRUD |

### 6.3 关键组件

| 组件 | 用途 |
|------|------|
| `RecipeCard.vue` | 菜谱卡片（封面+标题+标签+浏览/收藏数） |
| `AiChatDialog.vue` | AI 对话弹窗（SSE 流式渲染、Markdown 支持） |

### 6.4 交互规范

- **加载状态**：所有异步请求显示 Element Plus `Loading` 或 `Skeleton`
- **空状态**：列表无数据时显示插画+引导文案
- **错误提示**：使用 `ElMessage.error()` 顶部提示，3 秒自动消失
- **流式渲染**：AI 回复逐字渲染，配合光标动画；支持中断按钮
- **分页**：默认每页 20 条，提供页码跳转
- **响应式断点**：≥1200px 桌面、≥768px 平板、<768px 移动端

---

## 7. 数据模型

### 7.1 ER 关系总览

```
                       ┌──────┐
                       │ users│
                       └──┬───┘
                          │
        ┌─────────────────┼──────────────────────┐
        │                 │                      │
        ▼                 ▼                      ▼
   ┌─────────┐     ┌──────────┐         ┌───────────────┐
   │ recipes │◄────│user_favs │         │ai_conversations│
   └────┬────┘     │  history │         └───────┬───────┘
        │          └──────────┘                 │
        │                                        ▼
        ├──── recipe_tags ──► tags         ┌──────────┐
        │                                 │ai_messages│
        ├──── recipe_ingredients ──► ingredients └──────────┘
        │
        ├──── recipe_steps
        │
        ├──── recipe_reviews ◄── users
        │
        └──── meal_plan_items ──► meal_plans ◄── users

   独立表：cooking_notes (关联 users + 可选关联 recipes)
```

### 7.2 数据表清单（共 16 张表）

| 序号 | 表名 | 用途 | 关键字段 |
|------|------|------|---------|
| 1 | `users` | 用户表 | id, username, email, password_hash, role(user/admin), is_active, preferences(JSON: cuisines/diet_tags/free_text) |
| 2 | `tags` | 标签表 | id, name, type |
| 3 | `recipes` | 菜谱主表 | id, title, description, cover_image_url, difficulty, cooking_time, servings, estimated_cost, author_id, status(draft/pending/approved/rejected), reviewer_id, view_count, favorite_count, is_deleted |
| 4 | `recipe_tags` | 菜谱-标签关联 | recipe_id, tag_id（联合主键） |
| 5 | `ingredients` | 食材基础表 | id, name, category, image_url, nutrition(JSON: kcal/protein/fat/carbs), diet_tags(JSON: seafood/nuts/dairy/egg/gluten/mushroom/soy/allium/spicy/meat/veg) |
| 6 | `recipe_ingredients` | 菜谱-食材关联 | id, recipe_id, ingredient_id, quantity, note, sort_order |
| 7 | `recipe_steps` | 菜谱步骤表 | id, recipe_id, step_number, instruction, duration |
| 8 | `user_favorites` | 用户收藏表 | id, user_id, favorite_type(recipe/meal_plan), favorite_id |
| 9 | `user_browse_history` | 浏览历史 | id, user_id, recipe_id, meal_plan_id, viewed_at |
| 10 | `recipe_reviews` | 菜谱点评 | id, recipe_id, user_id, rating(1-5), content, is_deleted |
| 11 | `cooking_notes` | 烹饪心得 | id, user_id, title, content, related_recipe_id, images(JSON), is_public, view_count |
| 12 | `meal_plans` | 套餐主表 | id, user_id, title, description, cover_image_url, is_public, status, favorite_count |
| 13 | `meal_plan_items` | 套餐明细 | id, meal_plan_id, recipe_id, sort_order, note |
| 14 | `ai_conversations` | AI 会话 | id, user_id, title, created_at |
| 15 | `ai_messages` | AI 消息 | id, conversation_id, role(user/assistant/system), content, tokens |
| 16 | `cooking_note_comments` | 心得评论表 | id, note_id, user_id, content, is_deleted |

### 7.3 关键字段约束

| 表 | 字段 | 约束说明 |
|----|------|---------|
| users | username, email | UNIQUE |
| recipes | status | ENUM('draft','pending','approved','rejected') DEFAULT 'pending' |
| recipes | is_deleted | TINYINT DEFAULT 0（软删除标记） |
| user_favorites | (user_id, favorite_type, favorite_id) | UNIQUE（防重复收藏） |
| meal_plan_items | (meal_plan_id, recipe_id) | UNIQUE（同一套餐不重复加菜） |
| ai_messages | role | ENUM('user','assistant','system') |

### 7.4 索引设计

| 表 | 索引名 | 字段 | 用途 |
|----|--------|------|------|
| users | uk_username | username | 登录查询 |
| users | uk_email | email | 邮箱查询 |
| recipes | idx_status | status | 列表筛选 |
| recipes | idx_author_id | author_id | 作者菜谱查询 |
| recipes | idx_created_at | created_at | 时间排序 |
| recipes | idx_estimated_cost | estimated_cost | 预算筛选 |
| user_browse_history | idx_user_id, idx_viewed_at | user_id, viewed_at | 历史查询与热门统计 |
| ai_messages | idx_conversation_id | conversation_id | 会话消息查询 |

### 7.5 向量库数据结构（Chroma）

每条向量文档包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `recipe_{id}_{chunk_idx}` 或 `note_{id}_{chunk_idx}` |
| document | string | 结构化拼装的文本（标题+简介+标签+食材+步骤） |
| embedding | list[float] | BGE-M3 输出的 1024 维向量 |
| metadata.source_type | string | `recipe` 或 `cooking_note` |
| metadata.source_id | int | 源数据 ID |
| metadata.title | string | 标题 |
| metadata.tags | string | 标签（逗号分隔） |

---

## 8. 项目时间节点与里程碑

### 8.1 里程碑总览

| 里程碑 | 阶段目标 | 交付物 |
|--------|---------|--------|
| **M1** | 开题与需求分析 | 开题报告、PRD 文档 |
| **M2** | 技术选型与原型设计 | 技术方案、数据库设计、UI 原型 |
| **M3** | 后端核心模块开发 | 菜谱/用户/互动模块 API |
| **M4** | RAG 与 AI 模块开发 | 向量库构建、AI 对话、智能推荐 |
| **M5** | 前端开发完成 | 全部页面与组件 |
| **M6** | 联调与测试 | 测试报告、Bug 修复 |
| **M7** | 论文撰写与答辩 | 毕业论文、答辩 PPT、系统演示 |

### 8.2 详细时间节点

| 阶段 | 主要任务 | 产出 |
|------|---------|------|
| **阶段一：需求与设计** | 文献调研、开题报告、PRD 编写、数据库设计、UI 原型 | 开题.md、PRD.md、init.sql、原型图 |
| **阶段二：环境搭建与基础后端** | 项目初始化、配置中心、用户认证、菜谱 CRUD | FastAPI 项目骨架、auth/recipes 模块 |
| **阶段三：互动与套餐模块** | 点评、心得、套餐、收藏、历史 | reviews/cooking_notes/meal_plans/users 模块 |
| **阶段四：RAG 与 AI 助手** | Chroma 集成、Embedding 封装、向量同步、流式对话 | rag_service、ai_service、ai 模块 |
| **阶段五：智能推荐** | 个性化推荐、预算推荐、自然语言推荐、流式推荐 | recommendations 模块 |
| **阶段六：后台管理** | 审核、用户管理、标签/食材管理 | admin 模块及前端管理页面 |
| **阶段七：前端开发** | 路由、状态、所有页面与组件 | Vue3 前端完整应用 |
| **阶段八：数据填充与向量构建** | 菜谱数据导入、封面图生成、向量库全量重建 | import_data 脚本、chroma_db |
| **阶段九：联调与测试** | 前后端联调、性能测试、Bug 修复 | 测试报告 |
| **阶段十：论文与答辩** | 毕业论文撰写、答辩 PPT、演示视频 | 毕业论文、答辩材料 |

### 8.3 验收标准

| 验收项 | 标准 |
|--------|------|
| 功能完整性 | PRD 中 P0 功能 100% 实现，P1 功能 ≥ 90% 实现 |
| 接口文档 | FastAPI 自动生成的 `/docs` 可访问且与实现一致 |
| 数据库 | `init.sql` 可一键初始化，16 张表结构正确 |
| 向量库 | 全量重建脚本能成功运行，支持断点续传 |
| AI 对话 | 流式输出正常，多轮上下文连续 |
| 性能 | 主要接口响应时间符合 3.1 节要求 |
| 兼容性 | 主流浏览器与分辨率下 UI 正常 |
| 文档 | PRD、README、IMPORT_GUIDE 齐全 |

---

## 9. 风险评估及应对策略

### 9.1 技术风险

| 风险编号 | 风险描述 | 影响程度 | 发生概率 | 应对策略 |
|---------|---------|---------|---------|---------|
| R-T01 | LLM/Embedding API 服务不稳定或额度耗尽 | 高 | 中 | 已实现零向量兜底 + 关键词 n-gram 降级匹配；全量重建支持 checkpoint 断点续传；可切换其他供应商 |
| R-T02 | Chroma 向量库版本兼容性问题（langchain 1.x + chromadb 1.x） | 中 | 中 | 直接使用 `chromadb.PersistentClient` 绕过 LangChain 封装；锁定依赖版本（requirements.txt） |
| R-T03 | SSE 流式连接被代理/防火墙断开 | 中 | 低 | 设置 `Connection: keep-alive`、`Cache-Control: no-cache` 头；前端实现自动重连与超时检测 |
| R-T04 | MySQL 大数据量下查询性能下降 | 中 | 低 | 已为核心查询字段建立索引；列表查询使用 `joinedload` 避免 N+1；分页限制 page_size ≤ 100 |
| R-T05 | 前端 Markdown 渲染存在 XSS 风险 | 中 | 低 | `marked` 配合 sanitize 策略；用户输入不直接 innerHTML |
| R-T06 | 图片源站防盗链导致封面加载失败 | 中 | 中 | 封面图严格限制白名单（meishichina/xiachufang/douguo/xiangha）；URL 入库前做 HEAD 可达性校验；提供默认封面回退 |
| R-T07 | 多用户并发导致 SQLite 式的 Chroma 写冲突 | 低 | 低 | Chroma 本身支持多读单写；写操作集中在数据导入阶段；运行期以读为主 |

### 9.2 项目管理风险

| 风险编号 | 风险描述 | 影响程度 | 发生概率 | 应对策略 |
|---------|---------|---------|---------|---------|
| R-P01 | 个人开发周期紧张，功能膨胀 | 高 | 中 | 严格按 P0/P1/P2 优先级推进；P2 功能视时间弹性裁剪 |
| R-P02 | AI 模型效果不达预期（推荐不准、回答跑题） | 中 | 中 | 调整 RAG Top-K、Prompt 模板、temperature；引入 source_type 过滤提升检索精度 |
| R-P03 | 数据集规模不足导致推荐效果差 | 中 | 中 | 已编写 `import_recipes.py` 批量导入脚本；当前已积累 600+ 菜谱封面图资源 |
| R-P04 | 论文与开发时间冲突 | 中 | 中 | 开发过程同步记录设计文档（如代码内"面向答辩"注释）；阶段交付物即论文素材 |

### 9.3 数据安全风险

| 风险编号 | 风险描述 | 影响程度 | 发生概率 | 应对策略 |
|---------|---------|---------|---------|---------|
| R-D01 | API Key 泄露（硬编码入 git） | 高 | 低 | `.env.example` 仅提供占位；真实 Key 存于 `.env`（已 .gitignore）；`config.py` 提供生产环境修改提醒 |
| R-D02 | 用户密码明文泄露 | 高 | 低 | bcrypt 哈希存储；passlib 集成 |
| R-D03 | JWT 伪造攻击 | 中 | 低 | HS256 + 强随机密钥；24h 过期；后续可加 refresh token |
| R-D04 | 越权访问他人资源 | 高 | 中 | 所有用户中心接口校验 `user_id` 所有权；管理接口校验 `role=admin` |

### 9.4 应急预案

| 场景 | 应急方案 |
|------|---------|
| Mimo API 不可用（LLM） | AI 流式对话返回"服务暂不可用"提示；候选菜谱仍可展示 |
| SiliconFlow API 不可用（Embedding/Rerank） | Embedding 失败零向量兜底→关键词匹配降级；Rerank 失败/超时自动退回召回原序；检索主链路不中断 |
| MySQL 宕机 | 健康检查 `/api/health` 探测；启动备用只读实例（如条件允许） |
| Chroma 数据损坏 | 使用 `rebuild_vectorstore(resume=False)` 全量重建；checkpoint 文件可清理 |
| 服务器磁盘满 | Chroma 持久化目录与日志目录监控；定期清理过期日志 |

---

## 附录 A：API 端点清单

| 模块 | 前缀 | 主要端点 |
|------|------|---------|
| 认证 | `/api/auth` | POST /register, POST /login, GET /me |
| 菜谱 | `/api/recipes` | GET /, GET /hot, GET /{id}, POST /, PUT /{id}, DELETE /{id} |
| 用户中心 | `/api/users` | GET /profile, PUT /profile, GET /favorites, GET /history |
| 点评 | `/api/reviews` | GET /recipe/{id}, POST /, DELETE /{id} |
| 烹饪心得 | `/api/cooking-notes` | GET /, GET /{id}, POST /, PUT /{id}, DELETE /{id} |
| 套餐 | `/api/meal-plans` | GET /, GET /{id}, POST /, PUT /{id}, DELETE /{id} |
| 统计 | `/api/stats` | GET /（首页菜谱/套餐/用户总量 + 近 7 天新增） |
| AI 助手 | `/api/ai` | POST /chat (SSE), GET /conversations, DELETE /conversations/{id} |
| 智能推荐 | `/api/recommendations` | GET /personalized, GET /meal-plans, GET /prompts, POST /query |
| 后台管理 | `/api/admin` | GET /dashboard, POST /recipes/{id}/audit, POST /meal-plans/{id}/audit, GET /users, ... |
| 健康检查 | `/api/health` | GET / |

---

## 附录 B：环境配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| DB_HOST | localhost | MySQL 主机 |
| DB_PORT | 3308 | MySQL 端口 |
| DB_NAME | recipe_system | 数据库名 |
| JWT_SECRET_KEY | （占位） | 生产环境必须替换 |
| JWT_ACCESS_TOKEN_EXPIRE_MINUTES | 1440 | 24 小时 |
| CHROMA_PERSIST_DIR | F:/chroma_db | 向量库持久化目录（纯ASCII路径，避免Windows中文路径问题） |
| EMBEDDING_MODEL | BAAI/bge-m3 | 1024 维多语言 |
| LLM_MODEL | mimo-v2.5 | 小米 Mimo 对话模型；LLM_PROVIDER=mimo 时自动关闭深度思考 |
| LLM_TEMPERATURE | 0.7 | 平衡创造性与稳定性 |
| LLM_MAX_TOKENS | 4096 | 单次回复上限（≥4096，避免完整菜单被截断） |
| LLM_DISABLE_THINKING | True | 关闭 MiMo 等默认深度思考（reasoning_content） |
| RAG_TOP_K | 5 | 基础检索返回文档块数 |
| RAG_CHAT_RECALL_K | 40 | 对话RAG向量召回候选分块数（召回率优先） |
| RAG_CHAT_MAX_DISHES | 12 | 去重后喂给模型的菜谱上限 |
| RERANK_ENABLED | true | 精排总开关；失败/超时自动降级为召回排序 |
| RERANK_MODEL | BAAI/bge-reranker-v2-m3 | SiliconFlow 交叉编码精排模型（复用 Embedding Key） |
| RERANK_ALPHA | 0.5 | 分数融合权重：α×精排分+(1−α)×召回位置分；0=纯召回序，1=纯精排序（论文实验确定 0.5 为峰值） |
| RAG_CHUNK_SIZE | 500 | 分块字符数 |
| RAG_CHUNK_OVERLAP | 50 | 相邻块重叠 |
| RAG_EMBEDDING_BATCH_SIZE | 64 | 单次 Embedding 批次 |

---

**文档结束**

本 PRD 基于 [开题.md](file:///f:/毕业设计/Project/开题.md) 编写，与实际项目代码结构（[backend/app/main.py](file:///f:/毕业设计/Project/backend/app/main.py)、[SQL/init.sql](file:///f:/毕业设计/Project/SQL/init.sql)、[backend/app/config.py](file:///f:/毕业设计/Project/backend/app/config.py)）保持一致，可作为后续开发与验收的依据。
